"""
e_voting — National E-Voting System core package.

Sub-packages:
  models/    — Domain entity classes (Candidate, Voter, Poll, etc.)
  services/  — Business logic layer (one service per domain area)
  ui/        — Console-based presentation layer

Top-level modules:
  constants  — All configurable constants (replaces magic numbers)
  store      — Central data persistence and session management
"""
